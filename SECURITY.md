# Security Notes

This repository must not contain API keys, Helix passwords, Codex/OpenAI credentials, browser profile data, extension private keys, or local memory stores.

The local bridge reads credentials only from the browser session or environment variables during runtime. Review files before publishing to any public repository.
