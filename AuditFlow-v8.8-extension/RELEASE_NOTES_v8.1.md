# AuditFlow v8.1.0

AuditFlow 8.1 聚焦文件条目分类和可复核证据溯源。AI 只生成候选分类和评审意见；人工评级、证据确认、批准、基线、变更和关闭决定不会被 AI 自动覆盖。

## 证据溯源工作台

- 左侧目录按 `Process → PA → BP/GP` 的层级展开，用于选择当前正式评审指标。
- 中间 Trace 区显示已保存的证据关系，并提供备注、收藏、浏览中、差评、好评、有疑问六种一键标记。
- 中间区顶部可搜索证据关系，并使用文件归属页签切换来源文件。
- 右侧 Evidence Inventory 以表格展示 `ID/locator`、`Item`、`Type`、`File class`、`Rank`、`Process` 和关联动作。
- 右侧支持 item/文件/内容搜索及 Type、File class、Rank A/B/C 筛选。

## 文件和条目分类

解析器会先给出候选类别，评估师可逐条改定：

| 级别 | 可选值 |
| --- | --- |
| 文件类别 | 评审问题/记录、需求文件、流程性文件、测试文件、追溯表格文件 |
| 条目类型 | Information、Requirement、Process、Heading |

- 设置页可新增关键词分类规则。
- 自定义规则可同时指定文件类别、item 类型和 ASPICE 过程候选；规则只提供候选，评估师仍需确认正式过程归属。
- 完成基础分类后，AI 可为 item 提供更细的 ASPICE BP/GP 子过程候选，但不会代替评估师确认。
- 每个原子条目右侧都有删除按钮。删除 item 会保留上传文件和其他条目，并将操作写入审计轨迹。

## 导航与其他工作流

- 顶部第一行导航保留原有图标和功能，图标右侧显示简短功能名。
- `项目概况` 已更名为 `评审进度` / `Review progress`。
- Codex 评估助手使用配置的本地 Windows Codex bridge URL，聊天和项目级意见保持建议性。
- BP/GP 编辑锁仅限当前评估资源，关联证据仍可读并可独立编辑。
- 支持下载 CSV 问题收集模板、在线问题收集，以及带附件的反馈存储。

## 发布与部署

- 浏览器扩展与后端引擎版本：`8.1.0`。
- 扩展名称：`AuditFlow v8.1`。
- 工作区数据库迁移版本：`39`。
- 公网环境当前仍为 [http://120.25.197.24/](http://120.25.197.24/)。在域名备案和 HTTPS 证书配置完成前，不宣称 HTTPS 可用。

## 发布校验

`npm run check` 执行语法检查、发布 smoke 和协作持久化 smoke。发布 smoke 会精确校验 5 种文件类别、4 种 item 类型、6 种关系标记、层级 Trace 目录、搜索/类型/类别/rank 筛选、条目删除和“评审进度”导航标签。
