# AuditFlow v6.6 release notes

## ASPICE 正确性

- 本地“AI 评估”不再使用模板轮转。无证据默认 N；仅文件索引/元数据不超过 P+；只有一条直接证据不超过 L-；两条以上直接证据且证据充分才可达到 L/L+。F 仅保留给模型复核确认跨周期稳定闭环后的人工结论。
- PA 聚合增加否决门禁：任一 BP/GP 为 N 或 P 时该 PA 不能达到 L；任一 BP 未达 F 时 PA 1.1 不能达到 F。修复“5×L + 1×N 平均后仍通过 CL2”的缺陷。
- 补齐 SYS.1/SYS.2/SYS.4/SYS.5、HWE.1~4、MLE.1~4、ACQ.4、SPL.2、REU.2、PIM.3 的 BP 文本与证据指引，过程 BP 库覆盖 28/28。
- 目标 Level 3 时给出明确提示：本地引擎能力上限为 CL2。

## 性能

- 启动：版本一致时不再执行整库 JSON 序列化回写，避免每次打开卡顿。
- 保存：200ms 防抖合并写，页面隐藏/关闭时强制落盘，不丢数据。
- 渲染：项目内页签切换只重绘内容区；Dashboard 5 秒轮询带数据指纹脏检查；计划/日程搜索 150ms 防抖；派生指标按渲染批次记忆化。
- 页面加载：外部脚本 defer，页面外壳先绘制。

## 隐私与配置

- backend-config.json 默认 ai.enabled=false，baseUrl 默认企业网关；未显式配置时不会向外部发送证据摘录。

## 升级

- 工作区数据库 v23；首次打开自动迁移，不重建现有评估结果。
