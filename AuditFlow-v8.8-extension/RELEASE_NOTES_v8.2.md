# AuditFlow v8.2.0

## 公共入口与下载

- 公网根路径提供白色下载首页，按“插件 / Windows 便携版 / 网页版”分流。
- 网页版入口只负责进入 AuditFlow Web 应用；当服务启用 MySQL 认证时，应用页再显示登录/注册界面。
- 保持 HTTP-only 部署边界，不在本版宣称 HTTPS 或自动开通外部服务。

## 评审工作台

- 顶部导航保留图标，并显示两字短标签：评估、自定义、最近、应用、计划、空间、追踪、审查、缺陷、实践、变更、更多。
- 证据溯源三栏和逐条评审三栏均支持桌面端拖动分界线调整宽度；宽度只保存于当前浏览器，不上传到协作服务。
- 小屏自动回退为纵向布局，避免拖动状态挤压证据表和评审内容。

## 证据与 ASPICE 护栏

- 保留 Process → PA → BP/GP 层级目录、关系标记、证据 item 表格和五类文件/四类 item 分类。
- 删除单条 item 仍保留源文件、其他条目和审计轨迹；AI 只提供候选，正式评分由评估师确认。

## 验证

- `npm run check`：52 项 v8.2 发布 smoke 和 collaboration smoke 全部通过。
